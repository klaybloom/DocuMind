<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="documind"/>

    <!-- 控制台日志，包含 traceId，便于按请求排查问题。 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{traceId:-}] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 生产环境结构化 JSON 日志，方便日志平台采集。 -->
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>{"timestamp":"%d{yyyy-MM-dd'T'HH:mm:ss.SSSZ}","level":"%level","logger":"%logger{36}","thread":"%thread","traceId":"%X{traceId:-}","message":"%msg"}%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 默认使用控制台日志。 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- prod profile 使用 JSON 结构化日志，并降低框架日志噪声。 -->
    <springProfile name="prod">
        <root level="WARN">
            <appender-ref ref="JSON"/>
        </root>
        <logger name="com.documind" level="WARN"/>
        <logger name="dev.langchain4j" level="WARN"/>
        <logger name="org.springframework" level="WARN"/>
        <logger name="org.springframework.security" level="WARN"/>
    </springProfile>

    <!-- 非生产环境保留应用 INFO 日志，方便本地开发观察行为。 -->
    <springProfile name="!prod">
        <logger name="com.documind" level="INFO"/>
        <logger name="dev.langchain4j" level="WARN"/>
    </springProfile>

</configuration>
